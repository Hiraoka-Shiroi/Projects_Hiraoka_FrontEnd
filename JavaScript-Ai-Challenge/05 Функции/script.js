// =====================================================
// 05. ФУНКЦИИ
// =====================================================

// Функция - это кусок кода, который можно запускать много раз.

function sayHello(name) {
    console.log("Привет, " + name);
}

sayHello("Алишер");
sayHello("Мадина");

// -----------------------------------------------------
// return
// -----------------------------------------------------

// return возвращает результат из функции.

function add(a, b) {
    return a + b;
}

let result = add(5, 3);
console.log("Результат:", result);

// -----------------------------------------------------
// Функция с условием
// -----------------------------------------------------

function checkAge(age) {
    if (age >= 18) {
        return "Можно войти";
    } else {
        return "Нельзя войти";
    }
}

console.log(checkAge(20));
console.log(checkAge(15));

// =====================================================
// ЗАДАЧИ
// =====================================================

// Задача 1
// Создай функцию multiply(a, b),
// которая возвращает произведение двух чисел.

// Пример:
// console.log(multiply(4, 5)); // 20

// Твой код:


// Задача 2
// Создай функцию isEven(number),
// которая возвращает true, если число четное,
// и false, если число нечетное.

// Подсказка:
// number % 2 === 0

// Твой код:


// Задача 3
// Создай функцию getMax(a, b),
// которая возвращает большее число.

// Твой код:

