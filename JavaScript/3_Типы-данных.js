let name = "Hira"
let age = 23
let isStudent = true
let hasDriverLicense = false
let girlfriend = "Lima"
let height = 150

console.log(name + " is " + age + " years old. Is a student: " + isStudent + ", Has driver license: " + hasDriverLicense + ", Girlfriend: " + girlfriend + ", Height: " + height)

const a = "10";
const b = 5;

console.log(a + b); // 105

const a = "10";
const b = 5;

console.log(a - b); // 5, Вероятно число не может быть вычтено из строки, поэтому JavaScript пытается преобразовать строку в число.

typeof "Hiraoka"
typeof 21
typeof true
typeof false
// Вывод: String, Number, Boolean, Boolean

let test = "name"
let number = 8
console.log(test - number) // NaN, Результат не является числом, так как нельзя вычесть число из строки.